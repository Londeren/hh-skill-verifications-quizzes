## API — базовый уровень

🏆 Правильных ответов: 10 из 10

#### Q1. Какую задачу решают параметры пути в REST API запросах?

- [x] `Передают изменяющиеся части URL для доступа к конкретным ресурсам`

#### Q2. Какую задачу решает использование ReDoc при работе с API?

- [x] `Визуализирует и создает статическую документацию API на основе спецификации OpenAPI`

#### Q3. В чем заключается ключевое преимущество использования JWT для аутентификации в API по сравнению с традиционным подходом сессий в распределённых системах?

- [x] `JWT можно безопасно передавать между доменами, что удобно при работе с микросервисами`

#### Q4. Какое основное функциональное сходство имеют OAuth 2.0 и Basic Auth?

- [x] `Оба метода позволяют управлять доступом к ресурсам API`

#### Q5. Клиент успешно прошёл аутентификацию, но у него нет разрешения на выполнение операции с указанным ресурсом

Какой код ошибки вернёт сервер в этом случае?

- [x] `403`

#### Q6. Какое ключевое свойство метода POST следует учитывать при создании нового ресурса через API?

- [x] `POST не является идемпотентным методом, и повторный запрос может привести к созданию дубликатов`

#### Q7. Вы работаете с REST API и хотите обновить существующий ресурс. Вместо PUT или PATCH вы решили использовать POST

В чём основная проблема такого подхода?

- [x] `POST создаст новый ресурс вместо обновления существующего`

#### Q8. Какое преимущество использования параметра `offset` при реализации пагинации?

- [x] `Позволяет задавать смещение результатов относительно первой записи`

#### Q9. Какой из перечисленных методов обычно применяют для асинхронного обмена данными в реальном времени?

- [x] `WebSocket`

#### Q10. Какую задачу решает интеграция Swagger в проект с REST API?

- [x] `Облегчение разработки и тестирования API с предоставлением генерируемой документации и интерактивного интерфейса для тестирования запросов`
