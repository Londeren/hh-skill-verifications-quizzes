## HTML — продвинутый уровень

#### Что произойдет, если не добавить <!DOCTYPE> в документ HTML?

- [x] Браузер войдёт в режим совместимости, и страница будет показана с ограниченным функционалом

#### Выберите список, состоящий ТОЛЬКО из семантических тегов.

- [x] header, nav, main, article, section, aside

#### В каком варианте ответа правильно разделены на группы блочные и строчные элементы?

- [x] блочные: address, nav, img, h6, строчные: ul, a, big, output

#### Какое значение атрибута rel нужно указать в элементе <a>, чтобы не дать поисковому роботу индексировать активную ссылку?

- [x] nofollow

#### Выберите НЕВЕРНОЕ утверждение о теге `<iframe>`.

- [x] Можно запретить изменение размеров встроенного контента пользователем с помощью атрибута `noresize`

#### Какого атрибута НЕТ у тега `<meta>`?

- [x] description

#### Какой тег нужен для создания подвала таблицы?

- [x] tfoot

#### В чём разница между grid и flexbox?

- [x] Grid — это двумерная система компоновки, которая позволяет создавать сложные и гибкие конструкции как со строками, так и со столбцами. Flexbox — это одномерная система компоновки, фокусирующаяся на расположении элементов вдоль одной оси.

#### Выберите верное суждение о данном коде.

```
<!DOCTYPE html>
<html>
<head>
   <title>Пример HTML-формы</title>
</head>

<body>
   <form action="/submit" method="post">
      <label for="name">Имя:</label>

      <input type="text" id="name" name="name" required>
      <button type="submit">Отправить</button>
   </form>
</body>
</html>
```

- [x] В коде создается форма, отправляющая данные на сервер по адресу "/submit" методом POST. Форма содержит обязательное для заполнения поле ввода "Имя".

#### Выберите НЕВЕРНОЕ утверждение.

- [x] Семантическая разметка в HTML5 приводит к увеличению размера файлов и замедляет загрузку страницы

#### Что делает этот код?

```
<link hrеf="еverything.css" rеl="stylеsheet">
<!--[if IE]><link hrеf="hhru6.сss" rеl="stylеsheet"><![еndif]-->
```

- [x] Подключает основной CSS-файл everything.css для всех браузеров, кроме Internet Explorer. Для него подключается дополнительный CSS-файл hhru6.css.

#### Механизм, который позволяет создавать изолированные части веб-страницы, внутри которых могут находиться свои собственные стили и элементы, не влияющие на стили и элементы остальной страницы, — это…

- [x] Теневой DOM (Shadow DOM)

#### Вы добавляете видео и стандартные элементы управления видеоплеером. Какой ответ верный?

- [x] `html<video src="example.mp4" controls></video>`

#### Выберите НЕВЕРНОЕ утверждение о различиях XHTML и HTML.

- [x] В HTML можно использовать атрибуты class и id для стилизации и идентификации элементов, тогда как в XHTML нет такой возможности.

#### Как правильно встроить код JavaScript в файл HTML?

- [x] Чтобы включить код JS в файл HTML, можно использовать тег <script>. Можно либо встроить JS непосредственно в <script>, либо связать внешний файл JS с помощью атрибута src.

#### Какой HTML-тег используется в Web Components для создания местозаполнителя в пользовательском компоненте?

- [ ] `<insert>`
- [ ] `<placeholder>`
- [ ] `<yield>`
- [ ] `<template>`
- [x] `<slot>`

#### Вы хотите вставить изображение с поддержкой адаптивной подгрузки - например, WebP для новых браузеров и JPG как запасной вариант. Какой тег вам нужен?

- [ ] `<source>`
- [ ] `<img>`
- [ ] `<slot>`
- [ ] `<template>`
- [x] `<picture>`

#### В приложении, где шаблонные теги (<template>) используются для карусели изображений, какой вариант обеспечивает наилучшую доступность для технологий ассистирования при динамическом добавлении слайдов?

- [x] Обновлять `aria-live`-область после вставки клонированных шаблонов
- [ ] Клонировать все слайды заранее, скрывая невидимые через `display:none`
- [ ] Вставлять содержимое только в видимую область DOM
- [ ] Использовать `role=""region""` внутри шаблона

#### Вы создаёте форму, в которой значение ползунка должно отображаться в реальном времени при изменении. Какой тег применяется для отображения вычисленного значения?

- [x] `<output>`
- [ ] `<label>`
- [ ] `<datalist>`
- [ ] `<span>`
- [ ] `<value>`

#### Рассмотрите следующий код:

```html
<fieldset>
  <legend>Адрес</legend>
  <label for="city">Город: </label>
  <input id="city" type="text" />
  <label for="zip">Индекс: </label>
  <input type="text" />
</fieldset>
```

Что нужно исправить для улучшения доступности и правильной связи?

- [ ] Переместить `<legend>` внутрь `<label>`
- [x] Добавить атрибут `id="zip"` ко второму `<input>`
- [ ] Добавить `for="city"` к первой метке
- [ ] Присвоить оба `label` одному `input`
- [ ] Удалить `legend`, он не нужен

#### Проверяя форму оформления заказа, вы замечаете, что `<fieldset disabled>` содержит `<select required>`, которое браузер помечает как невалидное при сабмите. В чём причина и как исправить?

- [x] `disabled fieldset` отменяет `required` для всех наследуемых элементов, но браузеры реализуют поведение по-разному
- [ ] У `select` отсутствует дефолтное значение
- [ ] `<fieldset disabled>` отменяет required только для `input`
- [ ] Нужно переместить `select` вне `fieldset`

#### Вы создаёте набор вкладок. Какой набор атрибутов и ролей обеспечит корректную доступность вкладки?

- [ ] 1

```html
<div role="tabs">
  <div role="tab" selected>Вкладка 1</div>
  <div role="tab">Вкладка 2</div>
</div>
```

- [x] 2

```html
<div role="tablist">
  <div role="tab" tabindex="0" aria-selected="true">Вкладка 1</div>
  <div role="tab" tabindex="-1">Вкладка 2</div>
</div>
```

- [ ] 3

```html
<tabs>
  <tab active>Вкладка 1</tab>
  <tab>Вкладка 2</tab>
</tabs>
```

- [ ] 4

```html
<ul role="list">
  <li role="tabitem">Вкладка 1</li>
  <li>Вкладка 2</li>
</ul>
```

- [ ] 5

```html
<div aria-tablist="true">
  <div tabindex="0">Вкладка 1</div>
</div>
```

#### Рассмотрите следующий фрагмент разметки:

```html
<h1>Главная</h1>
<h3>0 компании</h3>
<h2>Новости</h2>
```

Какую проблему с точки зрения WCAG он содержит?

- [x] Нарушена логическая иерархия заголовков
- [ ] Заголовки не читаются скринридером
- [ ] Использованы устаревшие теги
- [ ] Не хватает `aria-level` у заголовков
- [ ] Следует заменить все заголовки на `h2`

#### Вы проектируете кастомную кнопку без использования ‹button> и создаёте такой фрагмент:

```html
<div role="button" tabindex="0" onclick="doAction()">Сохранить</div>
```

Как это влияет на доступность для пользователей с нарушениями моторики?

- [x] Элемент не обрабатывает клавишу Enter/Space
- [ ] Кнопка недоступна для фокусировки
- [ ] Мышь будет блокировать работу скринридера
- [ ] Всё реализовано корректно и не требует доработки
- [ ] Роль `button` работаеттолько на `<button>`

#### Какой из вариантов лучше минимизирует глубину DOM и при этом сохраняет читаемую структуру для простого текста с заголовком и описанием?

- [ ] `<div><span><h2>3аголовок</h2></span><span><р>Описание</р></span></div>`
- [ ] `<header><h2>3аголовок</h2></header><main><р>0писание</р></main>`
- [ ] `<article><div><h2>3аголовок</h2></div><div><р>0писание</р></div></article>`
- [x] `<section><h2>3аголовок</h2><р>0писание</р></section>`
- [ ] `<div><div><h2>3аголовок</h2><div><р>0писание</р></div></div></div>`

#### Как повлияет на построение DOM-дерева использование большого числа вложенных `template`-тегов внутри одного документа без атрибута `is` или `content`?

- [ ] Шаблонные теги игнорируются при создании DOM, не занимают место в дереве.
- [ ] Браузер удаляет все `template`-теги после построения дерева.
- [x] Добавит большое количество невидимых DOM-узлов, хотя они не рендерятся, но увеличивают объем `memory`.
- [ ] Все вложенные теги `template` трансформируются в `document fragment` только при использовании.

#### В приложении используется `<template>` для генерации карточек и их дальнейшего динамического наполнения из JS. Какой рисковый эффект может возникнуть при массовом клонировании, если шаблон содержит тяжелые SVG и deep-nested DOM?

- [ ] Потеря ссылок на экземпляры шаблонов
- [x] Значительное увеличение времени обработки клонирования и вставки в живой DOM
- [ ] Рекурсивные ошибки клонирования стилей
- [ ] Остановка event bubbling для вложенных элементов

#### Какой минимальный набор требований должен выполнять `custom element` для валидности его HTML-имени?

- [ ] Имя не длиннее 10 символов, обязательно число.
- [ ] Имя только заглавными, заканчивается на `-еl`.
- [ ] Имя только строчными буквами, начинается на `х-`, длина не менее 3 символов.
- [x] Имя содержит хотя бы один дефис, не содержит заглавных букв.

#### Рассмотрим следующий HTML-фрагмент с использованием Web Components:

```html
<my-card> <span slot="title">Заголовок</span> <р>Основной контент</р> </mycard>
```

Какой из следующих вариантов определения шаблона Web Component корректно отобразит переданный slot="title" внутри пользовательского элемента?

- [ ] `<template> <div class="card"> <slot></slot> <slot name="title"></slot> </div> </template>`
- [ ] `<template> <div class="card"> {{ title }} {{ default }} </div> </template>`
- [x] `<template> <div class="card"> <slot name="title"></slot> <slot></slot> </div> </template>`
- [ ] `<template> <div class="card"> <span slot="title"></span> <slot></slot> </div> </template>`
- [ ] `<template> <div class="card"> <slot title="title"></slot> <slot></slot> </div> </template>`

#### Рассмотрите следующий Web Component:

```html
<my-card>
<h2>3аголовок</h2>
<р>Описание</р>
</my-card>
```

И его шаблон:

```html
<template>
<div class="card">
<slot>/<slot>
</div>
</template>
```

Как отобразится контент внутри компонента?

- [ ] Только `<h2>` попадёт в шаблон
- [ ] Контент отобразится вне компонента
- [x] Весь вложенный контент будет вставлен в `<slot>`
- [ ] Только первый дочерний элемент попадёт в `slot`
- [ ] Ничего не отобразится без пате в `<slot>`
